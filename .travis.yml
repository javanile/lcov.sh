os: linux
group: stable
dist: trusty
language: generic

branches:
  only:
    - bats

env:
  - BASH_VERSION=4
  - BASH_VERSION=5

before_install:
  - bash <(curl -sL https://git.io/get-docker-compose)

script:
  - make test-travis

after_success:
  - bash <(curl -s https://codecov.io/bash)
